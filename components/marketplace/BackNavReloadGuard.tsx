// Deprecated — la lógica vive en `BackNavRefresh.tsx` (montado en RootLayout).
// Este archivo se mantiene como stub para no romper imports legacy.
export default function BackNavReloadGuard(): null {
  return null;
}
