// Stub for the `server-only` package in Vitest environments.
// In production Next.js, this package throws if imported from a client bundle.
// In tests we simply export nothing so the import resolves without throwing.
export {};
