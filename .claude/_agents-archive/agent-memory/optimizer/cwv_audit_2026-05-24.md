---
name: cwv-audit-2026-05-24
description: Auditoría rendering/CWV/hydration storefront Buleje — score 52/100 original; estado actualizado 2026-05-25 con 2 P0 resueltos y 5 P1/6 P2 vigentes
metadata:
  type: project
---

Score original: 52/100 (rendering/CWV) — auditado 2026-05-24.
Auditado nuevamente 2026-05-25. Informe completo: `reports/audit-2026-05-25/02-performance.md`.

## Resueltos (2026-05-25)

- **P0 marketplace waterfall**: `initialStores` L201 + `initialBestsellers` L203 ya pasan como props SSR a MarketplaceContent. TiendasDestacadas y BestsellersStrip tienen guard `if (initialItems && initialItems.length > 0) return` en useEffect — sin double-fetch.
- **P0 Google Fonts bloqueante**: preconnect en líneas 239-240 + `display=swap` en L243. Hero Image con `priority` en L280.
- **TrendingTodayWidget**: removido del árbol principal (solo en worktrees stale).
- **Admin TabRouter**: 44 `dynamic()` wrappers cubren todos los módulos.

## Hallazgos vigentes (2026-05-25)

1. **P1-01** — `app/t/[slug]/page.tsx:406` — `<img>` raw logo tenant hero desktop sin dimensiones → CLS + LCP candidate. Fix: `<Image fill sizes="128px" priority>`.
2. **P1-02** — `contexts/settings-context.tsx:174` — `fetch("/api/settings", { cache: "no-store" })` en mount. Fix: pasar settings como prop SSR desde layout.
3. **P1-03** — `contexts/promotions-context.tsx:24` — `fetch("/api/promotions")` en mount. Fix: prop SSR inicial.
4. **P1-04** — `app/checkout/entrega/page.tsx:253` — 3 useEffect seriales ubigeo (dep→prov→dist). Datos en memoria. Fix: `Cache-Control: max-age=86400` en endpoint + departamentos como prop SSR.
5. **P1-05** — `components/store/tenant/SectionRenderer.tsx:68` — `<img>` raw en sección editorial storefront sin dimensiones. Fix: `<Image fill sizes>`.
6. **P2-01** — `app/t/[slug]/page.tsx:243` — Google Fonts via stylesheet externa (tiene preconnect, pero no `next/font/google`). Fix: migrar a `next/font/google`.
7. **P2-02** — `app/(store)/mis-pedidos/page.tsx` — 1745 líneas client sin segmentar. Fix: dynamic() para OrderDetailModal y TrackingSection.
8. **P2-06** — 20 archivos `lib/db/*.db.ts` sin `"use cache"` (finance, transactions, analytics-cash-flow, campaigns, visitor-welcome, stats-live…).

**Why:** P1-01/05 son CLS directos. P1-02/03 suman ~350ms al mount de páginas públicas. P1-04 es el waterfall visible en checkout mobile.

**How to apply:** Al tocar /t/[slug], checkout/entrega, o StoreProviders — aplicar P1-01, P1-02/03, P1-04 primero.
